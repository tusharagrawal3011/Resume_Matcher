# Backend URL (server-side only — never exposed to the browser)
BACKEND_URL=http://127.0.0.1:3000

# API key for the backend (server-side only — never exposed to the browser)
API_KEY=rm_replace_with_secure_key
